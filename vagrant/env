#DEVELOPMENT-SPECIFIC ENV VARS

#SOURCE
#These are going into Vagrantfile, 
#run vagrant rsync-auto to sync while working
#should only be src code
SRC_DIR_TYPE="rsync"
SRC_DIR_HST="../../../tsad-proj"
SRC_DIR_GST="/tsad-proj"


#BINARIES (NOT SRC!)
#i put this folder outside of src bc rsync wasn't (actually) excluding it
#on my computer for some reason
BIGFILES_DIR="../../..//tsad-bigfiles"

IMAGES_DIR="$BIGFILES_DIR/images"
#Vagrant
BOXES_DIR="$IMAGES_DIR/vagrant"

#my boxes
#to make this box:
#1. in a dir: vagrant init yungsang/coreos
#2. vagrant up
#3. logged in once then logged out (not sure if it helps)
#4. vagrant repackage yungsang/coreos virtualbox 1.3.8
COREOS_URL="file://$BOXES_DIR/yungsang_coreos_virtualbox_138.box"
BOOT2DOCKER_URL="file://$BOXES_DIR/boot2docker-virtualbox.box"

#in the vagrantfile
DOCKER_BOX_ADD="192.168.123.111"
#DOCKER_BOX="COREOS"
DOCKER_BOX="COREOS"
DOCKER_URL=$(eval "echo \$"$DOCKER_BOX"_URL")

REGISTRY_BOX="BOOT2DOCKER"
REGISTRY_URL=$(eval "echo \$"$REGISTRY_BOX"_URL")


#Dockerregistry images
#created by: docker pull registry
#then: docker save registry > /path/to/image
REGISTRY_ADD="192.168.123.222"
REGISTRY_DIR="$IMAGES_DIR/docker-registry/"
#REGISTRY_IMG="$REGISTRY_DIR/registry.tar"


